export interface FormData {
    [key: string]: string
}

export interface ProfileDataInterface {
    id: number,
    email: string,
    first_name: string,
    last_name: string,
    is_active: boolean
}